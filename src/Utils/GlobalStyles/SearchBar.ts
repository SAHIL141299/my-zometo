import { CSSProperties } from "react";

export const searchBarConitainer = (): CSSProperties => {
  return {
    display: "block",
    margin: "0 auto",
    width: "50rem",
    backgroundColor: "red",
    position: "absolute",
  };
};
